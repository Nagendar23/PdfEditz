import cron from 'node-cron'
import fs from 'fs';
import path from 'path'

const cleanExpiredFiles = async(req,res)=>{
    try{
        const currentTime = new Date()
        const timeString = now.toLocaleTimeString();
        console.log(timeString)
    }
}